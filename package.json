{
  "name": "re-desk",
  "version": "0.2.0",
  "main": "index.js",
  "license": "Apache-2.0",
  "private": true,
  "scripts": {
    "lint": "eslint --ext .js,.jsx src static/queue/src",
    "test": "vitest run",
    "build:queue": "npm --prefix static/queue run build"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "vitest": "^4.1.9"
  },
  "dependencies": {
    "@forge/api": "^6.0.0",
    "@forge/resolver": "2.0.0"
  }
}
